# External Databases

1. MySQL → RDS
2. MongoDB → DocumentDB
3. Redis → ElastiCache
4. Elasticsearch → OpenSearch

Use init-scripts/ for DB initialization.
Use backup/ for backup and restore scripts.

